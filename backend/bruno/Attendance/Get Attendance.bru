meta {
  name: Get Attendance
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/attendance/{{attendanceRecordId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

vars:pre-request {
  baseUrl: http://localhost:3000
  attendanceRecordId: 7
}
